declare namespace MODEL {
  export interface IAuthentication {
    accessToken: string;
    refreshToken: string;
    expiresIn: number;
    access_token: string;
    refresh_token: string;
    expires_in: number;
  }
}
